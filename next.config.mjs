/** @type {import('next').NextConfig} */
const nextConfig = {
  // Other Next.js configuration goes here
  // e.g., transpileModules is not required if you don't need module transpiling
};

export default nextConfig;
